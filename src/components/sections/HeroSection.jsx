import { HeroBox } from '../HeroBox'

export const HeroSection = () => {
  return (
    <div id='home' className='relative h-screen min-h-[600px] flex justify-center'>
      <HeroBox />
    </div>
  )
}
